╔══════════════════════════════════════════════════════════════════════╗
║              中国农历新年节日特效 - 完整使用指南                       ║
╚══════════════════════════════════════════════════════════════════════╝

📦 项目文件列表
═══════════════════════════════════════════════════════════════════════

1. cny-festive-effects.html
   - 完整的演示页面（包含所有 HTML/CSS/JavaScript）
   - 可直接在浏览器中打开查看所有特效
   - 包含可视化控制面板

2. cny-festive-effects.js
   - 独立的 JavaScript 模块文件
   - 可集成到任何现有项目
   - 支持自定义配置选项

3. cny-integration-example.html
   - 集成示例页面
   - 展示如何在现有 HTML 中引入特效
   - 包含详细的使用注释

4. cny-quick-snippet.html
   - 最小化的代码片段
   - 压缩版本，体积最小
   - 适合快速复制粘贴

5. CNY-EFFECTS-README.md
   - 详细的使用文档
   - API 参考
   - 故障排除指南

6. 中国新年特效-使用指南.txt
   - 本文件（快速参考指南）


✨ 功能特性
═══════════════════════════════════════════════════════════════════════

✅ 烟花/爆竹效果
   - 彩色粒子爆炸动画
   - 自动随机触发
   - 支持手动触发

✅ 红包飘落
   - 从顶部飘落的红包
   - 可点击互动
   - 点击红包触发烟花

✅ 灯笼闪烁
   - 四角传统红灯笼
   - 呼吸灯效果
   - 显示"春福喜财"

✅ 纸屑飘落
   - 多彩节日纸屑
   - 多种形状（方形、圆形、三角形）
   - 旋转飘落动画

✅ 祝福语动画
   - 金色发光文字
   - 渐入渐出动画
   - 支持自定义祝福语


🚀 快速开始（3种方法）
═══════════════════════════════════════════════════════════════════════

方法 1：直接使用完整 HTML（最简单）
───────────────────────────────────────────────────────────────────────
1. 用浏览器打开 cny-festive-effects.html
2. 立即看到所有特效
3. 使用右上角控制面板测试功能

方法 2：集成到现有项目
───────────────────────────────────────────────────────────────────────
1. 将 cny-festive-effects.js 复制到你的项目文件夹

2. 在你的 HTML 中引入：
   <script src="cny-festive-effects.js"></script>

3. 添加初始化代码：
   <script>
       document.addEventListener('DOMContentLoaded', function() {
           const effects = new CNYFestiveEffects('index1');
       });
   </script>

4. 确保你有一个容器元素：
   <div id="index1">
       <!-- 你的页面内容 -->
   </div>

方法 3：快速代码片段（最快捷）
───────────────────────────────────────────────────────────────────────
1. 打开 cny-quick-snippet.html
2. 复制所有内容
3. 粘贴到你的 HTML 页面的 </body> 标签之前
4. 完成！


⚙️ 配置选项
═══════════════════════════════════════════════════════════════════════

基础配置：
const effects = new CNYFestiveEffects('index1', {
    enableFireworks: true,        // 启用烟花（默认: true）
    enableRedEnvelopes: true,     // 启用红包（默认: true）
    enableLanterns: true,         // 启用灯笼（默认: true）
    enableConfetti: true,         // 启用纸屑（默认: true）
    enableBlessings: true         // 启用祝福语（默认: true）
});

高级配置：
const effects = new CNYFestiveEffects('index1', {
    fireworksFrequency: 0.02,     // 烟花频率 (0-1，默认: 0.02)
    redEnvelopeInterval: 3000,    // 红包间隔毫秒（默认: 3000）
    confettiInterval: 400,        // 纸屑间隔毫秒（默认: 400）
    blessingInterval: 8000,       // 祝福语间隔毫秒（默认: 8000）
    customBlessings: [            // 自定义祝福语
        '新年快乐',
        '恭喜发财',
        '万事如意'
    ]
});


🎮 API 方法
═══════════════════════════════════════════════════════════════════════

effects.triggerFireworks(count)   // 触发烟花，count=数量（默认3）
effects.addBlessing(text)         // 显示祝福语，text=自定义文字（可选）
effects.pause()                   // 暂停所有特效
effects.resume()                  // 恢复所有特效
effects.toggle()                  // 切换暂停/继续状态
effects.destroy()                 // 销毁所有特效并清理资源

使用示例：
<button onclick="effects.triggerFireworks(5)">放5个烟花</button>
<button onclick="effects.addBlessing('龙年大吉')">显示祝福</button>
<button onclick="effects.toggle()">暂停/继续</button>


📝 常见使用场景
═══════════════════════════════════════════════════════════════════════

场景 1：网页顶部横幅特效
───────────────────────────────────────────────────────────────────────
<div id="header-banner" style="height: 300px; position: relative;">
    <!-- 横幅内容 -->
</div>

<script>
    const headerEffects = new CNYFestiveEffects('header-banner', {
        enableLanterns: false,      // 横幅区域不显示灯笼
        fireworksFrequency: 0.05    // 增加烟花频率
    });
</script>


场景 2：仅在特定时间启用
───────────────────────────────────────────────────────────────────────
<script>
    const now = new Date();
    const isSpringFestival = (now.getMonth() === 0 || now.getMonth() === 1);
    
    if (isSpringFestival) {
        const effects = new CNYFestiveEffects('index1');
    }
</script>


场景 3：用户点击按钮后启动
───────────────────────────────────────────────────────────────────────
<button id="startBtn">🎉 开启新年特效</button>

<script>
    let effects = null;
    document.getElementById('startBtn').addEventListener('click', function() {
        if (!effects) {
            effects = new CNYFestiveEffects('index1');
            this.textContent = '🛑 关闭特效';
        } else {
            effects.destroy();
            effects = null;
            this.textContent = '🎉 开启新年特效';
        }
    });
</script>


场景 4：移动设备性能优化
───────────────────────────────────────────────────────────────────────
<script>
    const isMobile = /Android|webOS|iPhone|iPad|iPod/i.test(navigator.userAgent);
    
    const effects = new CNYFestiveEffects('index1', {
        enableFireworks: !isMobile,     // 移动端禁用烟花
        fireworksFrequency: 0.01,       // 降低频率
        redEnvelopeInterval: 5000,      // 延长红包间隔
        confettiInterval: 800,          // 减少纸屑
        enableLanterns: !isMobile       // 移动端禁用灯笼
    });
</script>


场景 5：定时自动控制
───────────────────────────────────────────────────────────────────────
<script>
    const effects = new CNYFestiveEffects('index1');
    
    // 每10秒触发一次特别烟花
    setInterval(() => {
        effects.triggerFireworks(3);
        effects.addBlessing();
    }, 10000);
    
    // 在用户不活跃时暂停（节省资源）
    let idleTimer = null;
    document.addEventListener('mousemove', function() {
        effects.resume();
        clearTimeout(idleTimer);
        idleTimer = setTimeout(() => effects.pause(), 30000);
    });
</script>


🎨 样式自定义
═══════════════════════════════════════════════════════════════════════

所有样式类都使用 cny- 前缀，可以通过 CSS 覆盖：

/* 自定义红包大小和颜色 */
.cny-red-envelope {
    width: 60px !important;
    height: 80px !important;
    background: linear-gradient(135deg, #cc0000 0%, #880000 100%) !important;
}

/* 自定义红包上的文字 */
.cny-red-envelope::before {
    content: '财' !important;
    font-size: 32px !important;
}

/* 自定义祝福语样式 */
.cny-blessing-text {
    font-size: 60px !important;
    color: #ff0000 !important;
    font-family: "楷体", "KaiTi", serif !important;
}

/* 自定义灯笼颜色 */
.cny-lantern-body {
    background: radial-gradient(ellipse at center, #ff6600 0%, #cc3300 100%) !important;
}


⚡ 性能优化建议
═══════════════════════════════════════════════════════════════════════

1. 根据设备性能调整
   - 移动设备：禁用或减少特效
   - 低端设备：只启用1-2种特效

2. 合理设置生成间隔
   - 减少 redEnvelopeInterval 和 confettiInterval
   - 降低 fireworksFrequency

3. 及时清理资源
   - 页面切换时调用 destroy()
   - 使用完毕后销毁实例

4. 限制容器区域
   - 不要在整个页面启用
   - 只在特定区域使用特效


🐛 故障排除
═══════════════════════════════════════════════════════════════════════

问题：特效不显示
解决：
✓ 检查容器 ID 是否正确
✓ 确保容器有高度（至少 300px）
✓ 检查 JavaScript 是否在 DOMContentLoaded 后执行
✓ 打开浏览器控制台查看错误信息

问题：特效显示不全
解决：
✓ 确保容器 position 不是 static
✓ 检查容器是否有 overflow: hidden
✓ 调整容器的 z-index

问题：性能卡顿
解决：
✓ 降低特效频率
✓ 禁用部分特效
✓ 减少同时显示的元素数量
✓ 在性能较差的设备上禁用烟花

问题：样式冲突
解决：
✓ 使用 !important 覆盖样式
✓ 检查 CSS 选择器优先级
✓ 所有类名都有 cny- 前缀，不易冲突


🌐 浏览器兼容性
═══════════════════════════════════════════════════════════════════════

✅ Chrome 60+
✅ Firefox 55+
✅ Safari 11+
✅ Edge 79+
✅ Opera 47+
✅ 移动端浏览器（iOS Safari, Android Chrome）

不支持 IE 浏览器


📞 技术支持
═══════════════════════════════════════════════════════════════════════

如遇到问题，请：
1. 查看 CNY-EFFECTS-README.md 详细文档
2. 检查浏览器控制台的错误信息
3. 参考 cny-integration-example.html 示例代码


📄 文件大小
═══════════════════════════════════════════════════════════════════════

cny-festive-effects.js:   ~17 KB（未压缩）
cny-quick-snippet.html:   ~11 KB（压缩版）

总体非常轻量，不会影响页面加载速度。


🎊 最佳实践
═══════════════════════════════════════════════════════════════════════

1. ✅ 在页面加载完成后初始化
2. ✅ 使用自定义配置优化性能
3. ✅ 页面卸载时调用 destroy()
4. ✅ 根据节日时间动态启用
5. ✅ 提供用户关闭选项
6. ✅ 移动端适当减少特效
7. ✅ 测试不同浏览器的兼容性


═══════════════════════════════════════════════════════════════════════

                    祝您新年快乐，万事如意！
                         🎉 🧧 🏮 🎆 🎊

═══════════════════════════════════════════════════════════════════════

版本：v1.0.0
更新日期：2025-01-21
